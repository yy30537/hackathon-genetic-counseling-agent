---
description: Non-Device CDS 合规红线，全项目适用
alwaysApply: true
---

# 立身之本

本项目严格遵循 FDA「非医疗器械临床决策支持」豁免标准。定位是**专业医学指南的搬运工与翻译官**，
最终决策权完全交还专科医生。

**不诊断、不定性、不开药、不给剂量。** 越过这条线，产品丧失豁免资格。

# 禁用词（硬断言）

`comparisons[].explanation`、`vus_reassurance`、`next_steps` 三处禁止出现：

```
确诊    患有    即为    需服用    建议用药
```

两个坑：

- 扫描是**字面子串**匹配。即便用于否定句式（如「这不是确诊结论」）也判失败，须改写措辞绕开。
- 扫描范围**不含** `disclaimer` 与 `retrieved_chunks`——前者是法定免责原文，后者是知识库原文，允许出现这些词。

# 照本宣科

- 所有大模型输出必须由 RAG 检索切片支撑，**禁止自由发挥**。
- 检索无命中时 `comparisons` 返回**空数组**，`vus_reassurance` 明说未匹配到相关指南。
  宁可少说也不编造，不要把空上下文丢给模型。
- `next_steps` 必须同时含「儿童发育行为科」与「医学遗传科」——产品的落点是把用户交还给医生。

# 严禁开发（PRD §1.4 Won't Have）

登录注册、会话历史持久化、任何诊断结论或用药剂量建议、真实患者数据落库、多模态视频/眼动/音频分析。

# confidence_level 的措辞红线

该字段指**筛查系统自身匹配结果的可信度**，与「用户是否患病」无任何数学对应关系。
禁止表述为「得病概率」「患病概率」「可能性」「几率」，禁止用颜色梯度暗示「数值越高越可能得病」。
只能用「筛查可信度」「匹配可信度」等中性表述。
